# RstArticles
reStructuredText (rST) -> LaTeX -> PDF

The project can be installed through `pip install "rst_articles @ git+https://github.com/saisua/RstArticles"`,
and it is meant mainly to be run on Colab, by me